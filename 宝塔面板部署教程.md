# 宝塔面板部署教程

> 面向第一次部署的人。全程复制粘贴即可，预计 20 分钟。
> 示例服务器为 **Ubuntu 22 / Debian 12**，CentOS 7+ 命令等价。
> 文档中出现的 `你的域名` 请统一替换成你自己的域名（例如 `drive.example.com`）。

---

## 目录

- [一、准备](#一准备)
- [二、创建数据库](#二创建数据库)
- [三、部署后端](#三部署后端)
- [四、部署前端](#四部署前端)
- [五、配置站点与反向代理](#五配置站点与反向代理)
- [六、加固（可选但建议）](#六加固可选但建议)
- [七、部署完成检查清单](#七部署完成检查清单)
- [八、常见问题](#八常见问题)

---

## 一、准备

### 1.1 服务器

| 项目 | 建议 |
| --- | --- |
| 配置 | 2 核 2G 起步，网盘建议 2 核 4G + 大硬盘 |
| 系统 | Ubuntu 22.04 / Debian 12 / CentOS 7+ |
| 磁盘 | 按你要存的文件量决定，上传目录可以单独挂载 |

### 1.2 域名解析

到你的域名服务商，添加一条 **A 记录**，指向服务器公网 IP。

验证解析是否生效：

```bash
ping -c 2 你的域名
```

返回的 IP 等于你服务器 IP 才继续，否则后面申请 SSL 证书一定会失败。

### 1.3 安装宝塔面板

到宝塔官网 `https://www.bt.cn` 复制最新的安装命令，在服务器上以 **root** 执行。

安装完成后会输出面板地址、用户名和密码，**先记下来**。

> 如果迟迟装不上，通常是服务器没开放面板端口，去服务商的安全组放行。

### 1.4 在面板里安装运行环境

登录面板 → 左侧「软件商店」→ 安装以下三个：

| 软件 | 版本 | 说明 |
| --- | --- | --- |
| Nginx | 1.20 以上 | 反向代理 + 托管前端 |
| MySQL | 8.0 | 数据库 |
| Java 项目管理器 | 自带 JDK | 装完的 JDK 路径通常是 `/www/server/java/jdk-21.0.2/bin/java` |

> **JDK 版本必须是 21。** 装完可以在终端执行 `ls /www/server/java/` 确认目录名，
> 这个名字后面 `start.sh` 会自动去查找，对不上也没关系，它会依次尝试其它常见路径。

---

## 二、创建数据库

面板左侧「数据库」→「添加数据库」：

| 字段 | 填什么 |
| --- | --- |
| 数据库名 | `jdy_cloud` |
| 用户名 | `jdy_cloud` |
| 密码 | 点「随机」生成，**复制出来存好** |
| 权限 | 本地服务器（`localhost`） |

**建完先不要导入表结构**，等第三步上传完文件一起做。

---

## 三、部署后端

### 3.1 上传文件

在服务器上建目录：

```bash
mkdir -p /www/wwwroot/jdy-cloud/config
mkdir -p /www/wwwroot/jdy-cloud/logs
cd /www/wwwroot/jdy-cloud
```

需要放进这个目录的东西：

| 文件 | 从哪来 | 说明 |
| --- | --- | --- |
| `jdy-cloud.jar` | 你本机 `mvn clean package -DskipTests` 的产物 `target/jdy-cloud.jar` | 程序本体 |
| `start.sh` | 仓库的 `backend/start.sh.example` 改名而来 | 启动脚本 |
| `config/ironwall-rules.json` | 仓库的 `backend/config/ironwall-rules.sample.json` 改名而来 | 安全引擎规则 |

对应命令：

```bash
cd /www/wwwroot/jdy-cloud
cp /path/to/backend/start.sh.example ./start.sh
cp /path/to/backend/config/ironwall-rules.sample.json ./config/ironwall-rules.json
```

> 面板的「文件」功能可以直接拖拽上传，比命令行直观。
> **注意**：如果是从 Windows 上传 `start.sh`，先执行一次
> `sed -i 's/\r$//' start.sh`，否则会报 `$'\r': command not found`。

### 3.2 修改 start.sh

```bash
vi /www/wwwroot/jdy-cloud/start.sh
```

里面所有 `<<< 请填写 >>>` 都要换掉。**必填的只有四项**：

| 变量 | 填什么 |
| --- | --- |
| `DB_USERNAME` | 第二步建的数据库用户名，默认 `jdy_cloud` |
| `DB_PASSWORD` | 第二步生成的数据库密码 |
| `JWT_SECRET` | 执行 `openssl rand -hex 32` 生成的 64 位串，**每个站都要不一样** |
| `SITE_DOMAIN` | 你的域名，**不带 `https://`**、不带结尾斜杠 |

生成密钥：

```bash
openssl rand -hex 32
```

> 这四项有一个没填，脚本会打印缺了哪几项并**拒绝启动**——这是故意的，
> 免得站点带着半套配置对外服务。

顺便按需调整这两项（默认值即可，网盘建议调大）：

```bash
export MAX_FILE_SIZE="850MB"
export MAX_REQUEST_SIZE="900MB"
```

改完给执行权限：

```bash
chmod +x /www/wwwroot/jdy-cloud/start.sh
```

> **关于 `backend/.env.example`**
> 这个文件**只是字段对照表，程序不会读取它**。真正生效的是 `start.sh` 里的 `export`。
> 应用读取的是标准环境变量（`${DB_URL}`、`${JWT_SECRET}` 这种），
> 由 `start.sh` 在启动前导出。所以改配置请改 `start.sh`，不要改 `.env`。

### 3.3 导入数据库表结构

```bash
cd /www/wwwroot/jdy-cloud
mysql -u jdy_cloud -p jdy_cloud < /path/to/backend/src/main/resources/db/init.sql
```

也可以用面板：数据库 → 右侧「管理」进 phpMyAdmin → 选 `jdy_cloud` → 导入 → 选择
`init.sql` 上传。

> 这个脚本**只建表，不写入任何账号**。第一个管理员由 `start.sh` 里的
> `ADMIN_USERNAME` / `ADMIN_PASSWORD` / `ADMIN_EMAIL` 在首次启动时自动创建。
> 模板里给的是示例值 `admin` / `Admin@123456`，**正式上线前务必改成你自己的，并在首次登录后立即改密**。

### 3.4 启动后端

```bash
cd /www/wwwroot/jdy-cloud
./start.sh
```

看到这样的输出就是成功了：

```
[2026-01-01 12:00:00] 启动 jdy-cloud.jar（Java: /www/server/java/jdk-21.0.2/bin/java）
[2026-01-01 12:00:01] 已拉起进程 PID=12345，开始健康自检（最长 30 秒）
[2026-01-01 12:00:12] 经典云网盘启动成功（pid=12345）
```

验证：

```bash
curl -s http://127.0.0.1:15060/api/version/info
```

应返回：

```json
{"success":true,"message":"操作成功","data":{"current_version":"v1.48.0","changelog":[]}}
```

> 后端默认只监听 `127.0.0.1:15060`（`application.yml` 里 `server.address` 默认本机），
> **不需要在防火墙放行 15060**，外部访问统一走 nginx 反代。
> 想改端口改 `start.sh` 的 `SERVER_PORT`。

---

## 四、部署前端

前端有两种做法，**推荐第一种**。

### 做法一：本地构建后上传（推荐，服务器不用装 Node）

在你自己的电脑上：

```bash
cd frontend
cp .env.example .env.local
vi .env.local
```

`.env.local` 至少要填这几项：

| 变量 | 填什么 |
| --- | --- |
| `VITE_SITE_NAME` | 站点全称，显示在浏览器标题、页脚、登录页 |
| `VITE_SITE_SHORT_NAME` | 站点简称，2-6 个字 |
| `VITE_SITE_DOMAIN` | 你的域名，不带 `https://` |
| `VITE_CONTACT_EMAIL` | 对外联系邮箱 |

然后构建：

```bash
npm install
npm run build
```

产物在 `frontend/dist/`。把这个目录里的**全部内容**（不是 dist 文件夹本身）上传到
`/www/wwwroot/你的域名/`。

### 做法二：直接在服务器上构建

```bash
# 服务器先装 Node 18+（面板软件商店有）
cd /www/wwwroot
git clone <你的仓库地址> src
cd src/frontend
cp .env.example .env.local
vi .env.local
npm install
npm run build
cp -r dist/* /www/wwwroot/你的域名/
```

> `VITE_API_BASE_URL` **生产环境不用填**。前端调用的是相对路径 `/api`，
> 由 nginx 反向代理转给后端，同源部署不需要跨域配置。

---

## 五、配置站点与反向代理

### 5.1 创建站点

面板左侧「网站」→「添加站点」：

| 字段 | 填什么 |
| --- | --- |
| 域名 | 你的域名，可以同时填 `你的域名` 和 `www.你的域名` |
| 根目录 | 保持默认 `/www/wwwroot/你的域名` |
| FTP | 不创建 |
| 数据库 | 不创建（第二步已经建过） |
| PHP 版本 | **纯静态** |

建完把第四步的前端产物放进根目录，访问 `http://你的域名` 应该能看到首页。

### 5.2 添加反向代理

网站 → 点你的域名进「设置」→「反向代理」→「添加反向代理」：

| 字段 | 填什么 |
| --- | --- |
| 代理名称 | `api` |
| 目标 URL | `http://127.0.0.1:15060` |
| 发送域名 | `$host` |
| 代理目录 | `/api` |

添加完成后，打开面板生成的那个代理配置文件
（`/www/server/panel/vhost/nginx/proxy/你的域名/api.conf`），
把它调整成下面这样，否则**大文件上传会 413 失败**：

```nginx
location /api {
    proxy_pass http://127.0.0.1:15060;
    proxy_http_version 1.1;

    # 大文件上传：必须大于 start.sh 里的 MAX_REQUEST_SIZE
    client_max_body_size 1000m;
    client_body_timeout 300s;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
    proxy_request_buffering off;

    # 真实 IP 透传（后端由应用层统一解析，不信任伪造的 XFF）
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # 断点续传
    proxy_set_header Range $http_range;
    proxy_set_header If-Range $http_if_range;
    proxy_buffering off;
}
```

改完在面板里重载 nginx（网站 → 右上角「重载配置」），或命令行：

```bash
nginx -t && nginx -s reload
```

### 5.3 申请 SSL 证书

网站 → 你的域名 →「SSL」→「Let's Encrypt」→ 勾选域名 → 申请。

成功后打开「强制 HTTPS」。

验证：

```bash
curl -sI https://你的域名/ | head -1
curl -s  https://你的域名/api/version/info
```

第一个应返回 `HTTP/2 200`，第二个应返回版本号 JSON。

---

## 六、加固（可选但建议）

到这里站点已经能用了。下面这些是增强项，**不影响主流程**，可以先上线再补。

### 6.1 nginx 层安全段

仓库 `backend/deploy/` 下有三个可直接粘贴的片段：

| 文件 | 粘到哪 |
| --- | --- |
| `nginx-sensitive-paths-v1479.conf` | 站点 `server{}` 内，`location /` 之前 |
| `nginx-response-headers-v1479.conf` | 同上 |
| `nginx-http-zones-v1477.conf` | `/www/server/nginx/conf/nginx.conf` 的 `http{}` 段内 |

**顺序很重要**：`nginx-http-zones` 必须先加，否则站点里引用了限流 zone，
`nginx -t` 会报 `unknown limit_req_zone`。

粘完记得把里面的 `example.com` 换成你自己的域名：

```bash
cd /www/wwwroot && sed -i 's/example\.com/你的域名/g' 你的片段文件
```

### 6.2 服务器层护盾

仓库 `backend/server-guard/` 提供了 fail2ban、端口收敛、假端口陷阱、SSH 加固等脚本：

```bash
cd /path/to/backend/server-guard
bash install-server-shield.sh
```

装完回管理后台「安全预警」页，服务器前置护盾会点亮。

### 6.3 日志轮转

```bash
cp /path/to/backend/deploy/logrotate-jdy-cloud.conf /etc/logrotate.d/jdy-cloud
```

---

## 七、部署完成检查清单

逐条打勾，全绿就算部署成功：

- [ ] `curl -s http://127.0.0.1:15060/api/version/info` 返回版本号
- [ ] `curl -sI https://你的域名/` 返回 `HTTP/2 200`
- [ ] `curl -s https://你的域名/api/version/info` 返回版本号 JSON
- [ ] 浏览器打开 `https://你的域名` 能看到首页，且是 HTTPS
- [ ] 能注册新账号并登录
- [ ] 上传一个小文件成功
- [ ] 上传一个 500MB 以上的大文件成功（验证 413 配置）
- [ ] 创建一个公开分享链接，用无痕窗口打开能下载
- [ ] 管理后台能登录，仪表盘有数据
- [ ] 管理后台 → 安全预警，引擎显示「防护运行中」
- [ ] `logs/maintenance.flag` 在启动完成后已被删除

---

## 八、常见问题

**Q：`./start.sh` 打印「配置缺失」就不动了**

必填四项有没填的。注意 `JWT_SECRET` 长度不足 32 位也会被拒绝，用
`openssl rand -hex 32` 重新生成。

**Q：`$'\r': command not found`**

`start.sh` 是 Windows 换行。执行：

```bash
sed -i 's/\r$//' /www/wwwroot/jdy-cloud/start.sh
```

**Q：启动后 `curl 127.0.0.1:15060` 没反应**

看日志尾部：

```bash
tail -50 /www/wwwroot/jdy-cloud/logs/app.log
```

最常见的是数据库连不上——回去核对 `DB_USERNAME` / `DB_PASSWORD`，
以及数据库是不是只允许 `localhost` 连接。

**Q：上传大文件报 413**

nginx 的 `client_max_body_size` 没调大。见 [5.2](#52-添加反向代理)，
调成比 `start.sh` 里 `MAX_REQUEST_SIZE` 更大的值。

**Q：页面能打开但接口全部 404 或 502**

反代没配对。确认代理目录是 `/api`、目标是 `http://127.0.0.1:15060`，
并且后端确实在跑（`ss -ltnp | grep 15060`）。

**Q：重启服务器后站点不见了**

`start.sh` 不是开机自启的。在面板「计划任务」里加一条
**Shell 脚本**、周期选「每分钟」或「重启后」，执行：

```bash
pgrep -f jdy-cloud.jar > /dev/null || (cd /www/wwwroot/jdy-cloud && ./start.sh)
```

脚本自带端口占用保护，已经在跑就不会重复启动。

**Q：想换域名**

1. 面板改站点域名并重新申请证书
2. 改 `start.sh` 的 `SITE_DOMAIN` 后重启后端
3. 重新构建前端（`VITE_SITE_DOMAIN` 变了，需要重新 `npm run build`）

**Q：想升级版本**

```bash
cd /www/wwwroot/jdy-cloud
cp jdy-cloud.jar jdy-cloud.jar.bak
# 上传新的 jdy-cloud.jar 覆盖
pkill -f jdy-cloud.jar
sleep 2
./start.sh
```

`start.sh` 会在启动自检期间挂维护标识，重启窗口不会暴露给用户。

---

更多细节见仓库根目录 `README.md` 与 `backend/deploy/README.md`。
